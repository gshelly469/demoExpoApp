# demoExpoApp

## What’s included
- Expo (JavaScript) app
- Profile screen with header, top tabs (Posts, Clips, Tagged), image grid
- Mock data in `src/data`

## How to run
1. `git clone <repo>`
2. `cd demoExpoApp`
3. `npm install`
4. `npx expo start` ( open in simulator or Expo Go )
